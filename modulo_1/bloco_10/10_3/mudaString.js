const mudaString = (string) => string.toUpperCase();
const primeiraLetra = (string) => string[0];
const concatString = (s1, s2) => s1.concat(s2);

module.exports = {
  mudaString, 
  primeiraLetra, 
  concatString, 
}
